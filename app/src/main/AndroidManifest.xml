<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kiran.myapplication">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_square"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_circle"
        android:supportsRtl="true"
        android:theme="@style/Theme.Matrimony">
        <activity
            android:name=".Activities.Flash_page"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Activities.Develop"></activity>

        <activity android:name=".Activities.Login"></activity>
        <activity android:name=".Activities.Register"></activity>
        <activity android:name=".Fragments.Suggested_user"></activity>
        <activity android:name=".Fragments.Profile"></activity>
        <activity android:name=".Fragments.Homepage"></activity>


        <!--        <activity android:name=".Activities.Login"></activity>-->

    </application>

</manifest>